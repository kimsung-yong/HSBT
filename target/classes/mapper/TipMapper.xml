<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.TipMapper">
	<resultMap id="tipMap" type="domain.TipVO">
		<id property="t_no" column="t_no"/>
		<result property="t_title" column="t_title"/>
		<result property="t_content" column="t_content"/>
		<result property="u_no" column="u_no"/>
		<result property="t_regtime" column="t_regtime"/>
		<result property="t_updatetime" column="t_updatetime"/>
		<association property="userVO" column="id" resultMap="userMap"/>
	</resultMap>
	<resultMap id="userMap" type="domain.UserVO">
		<id property="u_no" column="u_no"/>
		<result property="id" column="id"/>
		<result property="pw" column="pw"/>
		<result property="phone" column="phone"/>
		<result property="name" column="name"/>
		<result property="address" column="address"/>
		<result property="manager" column="manager"/>
	</resultMap>

    <select id="getList" resultMap="tipMap">
		<![CDATA[
		select t.t_no, t.t_title, t.t_content, u.id, t.t_regtime, t.t_updatetime
		from tbl_tip t
		inner join tbl_user u
		on t.u_no = u.u_no
		where t_no > 0
		]]>
	</select>

	<insert id="insert">
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(tip_seq.nextval, #{t_title}, #{t_content}, #{u_no})
	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="t_no" order="BEFORE" resultType="long">
			select tip_seq.nextval from dual
		</selectKey>
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(#{t_no}, #{t_title}, #{t_content}, #{u_no})
	</insert>

	<select id="read" resultType="domain.TipVO">
		select t_no, t_title, t_content, u_no, t_regtime, t_updatetime
		from tbl_tip
		where t_no = #{t_no}
	</select>

	<delete id="delete">
		delete from tbl_tip where t_no = #{t_no}
	</delete>

	<update id="update">
		update tbl_tip
		set t_title = #{t_title},
		t_content = #{t_content},
		u_no = #{u_no},
		t_updatetime = sysdate
		where t_no = #{t_no}
	</update>
</mapper>