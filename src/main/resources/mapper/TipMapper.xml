<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.TipMapper">

    <select id="getList" resultType="domain.TipVO">
		<![CDATA[
		select t_no, t_title, t_content, u_no, t_regtime, t_updatetime
		from tbl_tip
		where t_no > 0
		]]>
	</select>

	<insert id="insert">
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(tip_seq.nextval, #{t_title}, #{t_content}, #{u_no})
	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="t_no" order="BEFORE" resultType="long">
			select tip_seq.nextval from dual
		</selectKey>
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(#{t_no}, #{t_title}, #{t_content}, #{u_no})
	</insert>

	<select id="read" resultType="domain.TipVO">
		select t_no, t_title, t_content, u_no, t_regtime, t_updatetime
		from tbl_tip
		where t_no = #{t_no}
	</select>

	<delete id="delete">
		delete from tbl_tip where t_no = #{t_no}
	</delete>

	<update id="update">
		update tbl_tip
		set t_title = #{t_title},
		t_content = #{t_content},
		u_no = #{u_no},
		t_updatetime = sysdate
		where t_no = #{t_no}
	</update>
</mapper>