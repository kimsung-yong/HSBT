<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.TipMapper">

    <select id="getList" resultType="domain.TipVO">
		<![CDATA[
		select * from tbl_tip where t_no > 0
		]]>
	</select>

	<insert id="insert">
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(tip_seq.nextval, #{tTitle}, #{tContent}, #{uNo})
	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="tNo" order="BEFORE" resultType="long">
			select tip_seq.nextval from dual
		</selectKey>
		insert into tbl_tip (t_no, t_title, t_content, u_no)
		values(#{tNo}, #{tTitle}, #{tContent}, #{uNo})
	</insert>

	<select id="read" resultType="domain.TipVO">
		select * from tbl_tip where t_no = #{tNO}
	</select>
</mapper>